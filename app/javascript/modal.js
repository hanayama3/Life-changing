$(function() {
    $('.habit-show').click(function() {
        // var habitIndex = $('.habit-show').index($(this));
        $('#habit-modal').fadeIn();
        // $('#habit-task').val(habitIndex)
    });
});